config/

# Grav runtime artifacts under user/ — installable via GPM, not content we author.
# Only user/pages/ is tracked (the editable example content).
user/accounts/
user/config/
user/data/
user/plugins/
user/themes/
!user/themes/quark2/css/custom.css
